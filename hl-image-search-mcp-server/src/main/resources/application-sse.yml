spring:
  ai:
    mcp:
      server:
        name: hl-image-search-mcp-server
        version: 1.0.0
        type: SYNC
        # sse
        stdio: false
  main:
    # SSE 模式需要保留 Web 容器，但关闭 Banner 可以减少无关日志干扰。
    banner-mode: off
